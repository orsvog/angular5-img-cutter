export enum SizeRuleEnum {
    contain,
    stretchByWidth,
    stretchByHeight,
    realsize
}

export enum ShapeEnum {
    rectangle,
    circle
}

export enum cropActionEnum {
    resize,
    drag
}
